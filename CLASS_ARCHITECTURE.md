# 클래스 및 아키텍처 설계 기준 문서

- 클래스가 다루는 데이터와 책임을 기준으로 설계한다.
- 클래스의 이름은 단수형을 선택했다. 객체를 생성할 수 없는 클래스의 이름은 보통 복수형 명사로 짓는 관례가 있기 때문이다.

## Layer 설계

- 처음부터 Layer를 도입하지는 않았습니다. 리팩토링을 거치며 객체지향, SRP에 신경쓰다 보니 자연스럽게 Layer가 도출되었습니다.

### Presentation Layer

- 비즈니스 로직 호출을 담당하고, View와 상호작용
- 의존관계를 단뱡향으로 유지하기 위해 MVC 패턴 도입

### Service Layer

- 유즈케이스의 오케스트레이션을 담당 (비즈니스 로직)
- Controller로 부터 비즈니스 로직의 관심사를 분리

### Domain

- 문제 영역에 대한 규칙을 다룸

**도메인 값 객체**

- 도메인 규칙 스스로 보장하며, “값” 자체로 동등함을 판단하는 객체

**도메인 서비스**

- 도메인 값 객체로만 해결하지 못하는 도메인의 문제를 해결
