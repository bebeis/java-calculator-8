# 문자열 덧셈 계산기

## 기능 요구사항

- 입력한 문자열에서 숫자를 추출하여 더하는 계산기를 구현한다.

### 요구사항 세부 규칙

- 기본 구분자
    - 쉼표(,) 또는 콜론(:)을 구분자로 가지는 문자열을 전달하는 경우 구분자를 기준으로 분리한 각 숫자의 합을 반환한다.
- 커스텀 구분자
    - 앞의 기본 구분자(쉼표, 콜론) 외에 커스텀 구분자를 지정할 수 있다. 커스텀 구분자는 문자열 앞부분의 "//"와 "\n" 사이에 위치하는 문자를 커스텀 구분자로 사용한다.
- 예외 규칙
    - 사용자가 잘못된 값을 입력할 경우 IllegalArgumentException을 발생시킨 후 애플리케이션은 종료되어야 한다.

## 해결해야 하는 문제

- 사용자 INPUT이 올바른지 검증한다.
- 커스텀 구분자를 추출한다.
- 구분자를 기준으로 숫자를 찾는다.
- 모든 숫자를 더한다.

## 명확하게 명시되지 않은 요구사항

- 커스텀 구분자와 기본 구분자가 겹치는 경우
    - 판단 : 예외 처리 없이 정상 동작한다.
- 커스텀 구분자가 숫자인 경우
    - 판단 : 예외를 발생시킨다. 숫자를 허용하면 답이 여러가지 나올 수 있다.
- 커스텀 구분자는 단순한 하나의 character 일까? String 일까?
    - 판단 : 요구사항은 여러 도메인 언어들이 섞인다. String도 허용한다.
- 커스텀 구분자가 공백 문자인 경우
    - 판단 : 공백 문자도 커스텀 구분자가 될 수 있다.
- 양수가 정수만 의미할까? 소수도 가능할까?
    - 판단 : 정수로 제한한다는 말이 없었으므로 소수도 가능하게 한다.
- 공백 문자열 처리
    - 판단 : ""이 0인 이유는, 기저 값인 0에 아무것도 더하지 않았기 때문이다. 공백 문자열이 값이 될 수는 없다.

### 문제 영역 세분화

- 입력한 문자열에서 숫자를 추출하여 더하는 계산기를 구현한다.
    - 구분자를 이용하여 숫자를 추출한다.
        - 커스텀 구분자가 존재한다면 커스텀 구분자를 추출한다.
            - 커스텀 구분자 영역이 존재한다면 커스텀 구분자 영역을 분리한다.
                - 커스텀 구분자 영역의 포맷을 검증한다.
            - 커스텀 구분자 영역에서 올바른 커스텀 구분자 값을 찾는다.
                - 커스텀 구분자를 검증한다.
        - 모든 구분자를 바탕으로 숫자를 추출한다.
            - 숫자 영역의 포맷을 검증한다.
            - 숫자 영역에서 올바른 숫자 값을 찾는다.
    - 숫자를 더한다.

## 시스템 해결 영역

- 사용자 INPUT이 올바르지 않은 경우 IllegalArgumentException을 발생시킨 후 애플리케이션은 종료되어야 한다.
- '구분자와 양수로 구성된 문자열'을 입력받는다.
- '덧셈 결과'를 출력한다.

