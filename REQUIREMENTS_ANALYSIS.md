# 문자열 덧셈 계산기

## 기능 요구사항

- 입력한 문자열에서 숫자를 추출하여 더하는 계산기를 구현한다.

### 요구사항 세부 규칙

- 기본 구분자
    - 쉼표(,) 또는 콜론(:)을 구분자로 가지는 문자열을 전달하는 경우 구분자를 기준으로 분리한 각 숫자의 합을 반환한다.
- 커스텀 구분자
    - 앞의 기본 구분자(쉼표, 콜론) 외에 커스텀 구분자를 지정할 수 있다. 커스텀 구분자는 문자열 앞부분의 "//"와 "\n" 사이에 위치하는 문자를 커스텀 구분자로 사용한다.
- 예외 규칙
    - 사용자가 잘못된 값을 입력할 경우 IllegalArgumentException을 발생시킨 후 애플리케이션은 종료되어야 한다.

## 해결해야 하는 문제

- 사용자 INPUT이 올바른지 검증한다.
- 커스텀 구분자를 추출한다.
- 구분자를 기준으로 숫자를 찾는다.
- 모든 숫자를 더한다.

## 명확하게 명시되지 않은 요구사항

- 커스텀 구분자와 기본 구분자가 겹치는 경우
    - 판단 : 커스텀 구분자 추가 없이 정상 진행한다.
- 커스텀 구분자가 숫자로만 구성된 경우
    - 판단 : 예외를 발생시킨다. 숫자로만 구성되면 답이 여러가지 나올 수 있다.
- 커스텀 구분자는 단순한 하나의 character 일까? String 일까?
    - 판단 : 요구사항은 여러 도메인 언어들이 섞인다. String도 허용한다.
- 커스텀 구분자가 공백 문자인 경우
    - 판단 : 공백 문자도 커스텀 구분자가 될 수 있다.
- 양수가 정수만 의미할까? 소수도 가능할까?
    - 판단 : 정수로 제한한다는 말이 없었으므로 소수도 가능하게 한다.
- 빈 문자열이 커스텀 구분자가 될 수 있을까?
    - 판단 : ""이 0인 이유는, 기저 값인 0에 아무것도 더하지 않았기 때문이다. 공백 문자열이 값이 될 수는 없다.
    - 모든 문자 사이가 분리점이 되버리므로, 커스텀 구분자에 공백인 경우 예외를 발생시킨다.
- 개행 문자("\n")가 커스텀 구분자가 될 수 있나?
    - 제시된 기능 요구사항 : **문자열 앞부분의** "//"와 "\n" 사이에 위치하는 문자를 커스텀 구분자로 사용한다.
    - 판단 : 최초 등장하는 "\n"까지라는 것이다. 따라서 개행문자는 커스텀 문자가 될 수 없다.
- 소수를 허용하는데 커스텀 구분자로 "."이 등장한다면?
    - 판단 : 정수 덧셈으로만 처리한다.
        - 예를 들어) 사용자가 커스텀 문자를 "."로 지정했다.
        - 사용자가 "1.1:1.2" 라고 적었다면, 1+1+1+2가 되어야 한다. "1.1 + 1.2"를 의도했다면 사용자 실수이다.
    - 다만 실제 실무였다면, 나는 기획자에게 다시 한 번 검토해달라고 물어봤을 것이다.
- 제어 문자 값이 온다면?
    - 판단 : 탭('t')을 제외한 제어 문자 입력 시 예외를 발생시킨다.
- 문자열이 너무 길다면?
    - 판단 : 최대 30 자로 제한한다.
    - 문자열이 길어질수록 split 하는데 오래 걸린다.

### 문제 영역 세분화

- 입력한 문자열에서 숫자를 추출하여 더하는 계산기를 구현한다.
    - 구분자를 이용하여 숫자를 추출한다.
        - 커스텀 구분자가 존재한다면 커스텀 구분자를 추출한다.
            - 커스텀 구분자 영역이 존재한다면 커스텀 구분자 영역을 분리한다.
                - 커스텀 구분자 영역의 포맷을 검증한다.
            - 커스텀 구분자 영역에서 올바른 커스텀 구분자 값을 찾는다.
                - 커스텀 구분자를 검증한다.
        - 모든 구분자를 바탕으로 숫자를 추출한다.
            - 숫자 영역의 포맷을 검증한다.
            - 숫자 영역에서 올바른 숫자 값을 찾는다.
    - 숫자를 더한다.

## 시스템 해결 영역

- 사용자 INPUT이 올바르지 않은 경우 IllegalArgumentException을 발생시킨 후 애플리케이션은 종료되어야 한다.
- '구분자와 양수로 구성된 문자열'을 입력받는다.
- '덧셈 결과'를 출력한다.

